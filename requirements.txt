# Training & inference (Colab T4 / CUDA)
torch>=2.2.0
transformers>=4.51.0
peft>=0.14.0
trl>=0.15.0
bitsandbytes>=0.45.0
accelerate>=1.2.0
datasets>=3.2.0
evaluate>=0.4.3
rouge-score>=0.1.2
bert-score>=0.3.13

# Guided generation
outlines>=0.1.11
pydantic>=2.10.0

# Demo
gradio>=5.12.0

# On-device inference (Apple Silicon Mac only — the Linux mlx wheel is broken
# and crashes transformers on Colab; markers skip it everywhere else)
mlx>=0.22.0; sys_platform == "darwin" and platform_machine == "arm64"
mlx-lm>=0.21.0; sys_platform == "darwin" and platform_machine == "arm64"

# Optional experiment tracking
# wandb>=0.19.0
